/// ⚙️ Coleção de configurações da aplicação
/// Path usado no app:
/// - AppInfo/{configName}
///   - GoogleAndroidMaps/Api_key: string
///   - GoogleMapsApiKey/Api_key: string
///
/// 🔒 SEGURANÇA:
/// - Leitura: permitida para usuários autenticados (necessário para inicialização)
/// - Escrita: bloqueada (apenas via console/Admin SDK)

match /AppInfo/{configName} {
  /// 📖 LEITURA:
  /// - Qualquer usuário autenticado pode ler configurações
  /// - Necessário para inicialização do app (mapas, etc)
  allow read: if isSignedIn();
  
  /// ❌ ESCRITA:
  /// - Bloqueada para usuários
  /// - Apenas via Firebase Console ou Admin SDK
  allow write: if false;
}
